import { PlantPlusValidators } from './plant-plus-validators';

describe('PlantPlusValidators', () => {
  it('should create an instance', () => {
    expect(new PlantPlusValidators()).toBeTruthy();
  });
});
